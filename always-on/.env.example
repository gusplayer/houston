## Bearer token clients must send when authenticating to the engine.
## Rotate if exposed. Desktop app stores its copy in ~/.squad/engine.json.
SQUAD_ENGINE_TOKEN=replace-me-with-a-long-random-string
